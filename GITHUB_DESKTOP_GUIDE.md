# Полная инструкция: Создание репозитория и загрузка кода через GitHub Desktop

## Подготовка

### Шаг 1: Установка GitHub Desktop

Если GitHub Desktop еще не установлен:

1. Скачайте с [desktop.github.com](https://desktop.github.com)
2. Установите приложение
3. Запустите и войдите в свой GitHub аккаунт (`tryvailo`)

---

## Создание нового репозитория через GitHub Desktop

### Шаг 2: Создать новый репозиторий

1. Откройте **GitHub Desktop**
2. В верхнем меню выберите: **`File` → `New Repository`**
   - Или используйте горячую клавишу: **`Ctrl+N`** (Windows/Linux) или **`Cmd+N`** (Mac)

### Шаг 3: Заполнить форму создания репозитория

В открывшемся окне заполните следующие поля:

#### Основные настройки:

- **Name:** `MKeeper`
  - Это имя репозитория на GitHub

- **Description:** `A free tool for dementia families to preserve their loved one's story`
  - Описание репозитория (опционально, но рекомендуется)

- **Local Path:** 
  - Нажмите кнопку **`Choose...`**
  - Выберите папку: `/Users/alexandertryvailo/Documents/Products/MKeeper`
  - Или введите путь вручную: `/Users/alexandertryvailo/Documents/Products/MKeeper`

#### Дополнительные настройки:

- **Git ignore:** 
  - Выберите **`None`** 
  - (У вас уже есть файл `.gitignore` в проекте)

- **License:**
  - Выберите **`None`**
  - (У вас уже есть файл `LICENSE` в проекте)

- **Initialize this repository with a README:**
  - ❌ **Снимите галочку** (у вас уже есть `README.md`)

### Шаг 4: Создать репозиторий

1. Нажмите кнопку **`Create Repository`**
2. GitHub Desktop:
   - Создаст репозиторий на GitHub
   - Инициализирует Git в вашей локальной папке
   - Автоматически подключит локальную папку к удаленному репозиторию

---

## Первый коммит и загрузка кода

### Шаг 5: Проверить файлы перед коммитом

После создания репозитория вы увидите список изменений в левой панели GitHub Desktop.

**Важно:** Проверьте, что в список **НЕ попадают**:
- ❌ `.env.local` (должен быть скрыт)
- ❌ `node_modules/` (должен быть скрыт)
- ❌ `.next/` (должен быть скрыт)
- ❌ `data/` (должен быть скрыт)
- ❌ `*-old.tsx` файлы (должны быть скрыты)

Если эти файлы видны — проверьте файл `.gitignore` в корне проекта.

### Шаг 6: Выбрать файлы для коммита

1. В левой панели GitHub Desktop вы увидите список всех файлов
2. Убедитесь, что **все нужные файлы отмечены галочкой** ✅
   - Если какие-то файлы не отмечены — отметьте их вручную
   - Если какие-то файлы не должны быть в репозитории — снимите галочки

### Шаг 7: Написать сообщение коммита

Внизу окна GitHub Desktop есть два поля:

#### Summary (обязательное):
```
Initial commit: Memory Keeper MVP
```

#### Description (опционально, но рекомендуется):
```
- Guided interview (32 questions, 5 categories)
- PDF export functionality
- Shareable links with expiration
- Email sharing
- Family member invitations
- Activity history
- Responsive design
```

### Шаг 8: Сделать коммит

1. Нажмите кнопку **`Commit to main`** внизу окна
2. Дождитесь завершения коммита
   - В правой панели вы увидите историю коммитов
   - Ваш коммит появится в списке

### Шаг 9: Отправить код на GitHub

1. В верхней панели GitHub Desktop нажмите кнопку **`Publish branch`**
   - Если кнопка называется **`Push origin`** — нажмите её
2. Дождитесь завершения загрузки
   - В правом нижнем углу вы увидите прогресс
   - Когда загрузка завершится, появится уведомление

---

## Проверка результата

### Шаг 10: Проверить на GitHub

1. Откройте браузер
2. Перейдите на: [https://github.com/tryvailo/MKeeper](https://github.com/tryvailo/MKeeper)
3. Убедитесь, что:
   - ✅ Все файлы загружены
   - ✅ `README.md` отображается корректно
   - ✅ `LICENSE` файл присутствует
   - ✅ `.env.example` виден
   - ✅ Нет файла `.env.local`
   - ✅ Нет папки `node_modules/`
   - ✅ Нет папки `data/`

---

## Дальнейшая работа

### Ежедневная работа с кодом:

1. **Вносите изменения** в файлы проекта
2. **GitHub Desktop автоматически отслеживает** изменения
3. **Выбирайте файлы** для коммита в левой панели
4. **Пишите сообщение коммита** внизу
5. **Нажимайте `Commit to main`**
6. **Нажимайте `Push origin`** для отправки на GitHub

### Просмотр истории:

- В правой панели GitHub Desktop вы видите историю всех коммитов
- Можно просмотреть изменения в любом коммите
- Можно откатить изменения, если нужно

---

## Решение проблем

### Проблема: "Repository not found"

**Решение:**
1. Проверьте, что вы вошли в правильный GitHub аккаунт (`tryvailo`)
2. В меню: `Repository` → `Repository Settings` → `Remote`
3. Убедитесь, что URL: `https://github.com/tryvailo/MKeeper.git`

### Проблема: Файлы не отправляются на GitHub

**Решение:**
1. Убедитесь, что файлы отмечены галочкой в левой панели
2. Проверьте, что вы сделали коммит (`Commit to main`)
3. Убедитесь, что нажали `Push origin` (или `Publish branch`)

### Проблема: Видны файлы, которые не должны быть в репозитории

**Решение:**
1. Проверьте файл `.gitignore` в корне проекта
2. Если файла нет или он неполный — добавьте нужные правила
3. После изменения `.gitignore` сделайте новый коммит

### Проблема: GitHub Desktop не видит изменения

**Решение:**
1. Нажмите `Ctrl+R` (Windows/Linux) или `Cmd+R` (Mac) для обновления
2. Или в меню: `Repository` → `Refresh`

---

## Альтернатива: Командная строка

Если предпочитаете командную строку, выполните:

```bash
# Перейти в папку проекта
cd /Users/alexandertryvailo/Documents/Products/Registry

# Инициализировать Git (если еще не инициализирован)
git init

# Добавить все файлы
git add .

# Сделать первый коммит
git commit -m "Initial commit: Memory Keeper MVP

- Guided interview (32 questions, 5 categories)
- PDF export functionality
- Shareable links with expiration
- Email sharing
- Family member invitations
- Activity history
- Responsive design"

# Подключить удаленный репозиторий
git remote add origin https://github.com/tryvailo/MemoryK.git

# Переименовать ветку в main
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

---

## Готово! 🎉

Теперь ваш код на GitHub, и вы можете:
- Просматривать код на GitHub
- Клонировать репозиторий на другие компьютеры
- Настроить автоматический деплой на Vercel
- Пригласить других разработчиков к сотрудничеству

