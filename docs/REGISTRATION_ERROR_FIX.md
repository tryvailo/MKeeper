# Исправление ошибки "Failed to fetch" при регистрации

## ✅ Проблема решена

**Ошибка:** `Failed to fetch` при попытке регистрации

**Причина:** В `.env.local` были placeholder значения вместо реальных Supabase credentials.

## 🔧 Что было исправлено:

### 1. Обновлены переменные окружения в `.env.local`:

**Было:**
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

**Стало:**
```
NEXT_PUBLIC_SUPABASE_URL=https://cfspakgblafqqqtxojpy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 2. Улучшена обработка ошибок в `lib/supabase/client.ts`:

Добавлена проверка наличия переменных окружения с понятным сообщением об ошибке.

## 🧪 Тестирование:

1. **Перезапустите сервер** (если еще не перезапустили):
   ```bash
   # Остановите текущий процесс (Ctrl+C)
   npm run dev
   ```

2. **Откройте в браузере:** `http://localhost:3000/sign-up`

3. **Попробуйте зарегистрироваться:**
   - Email: ваш тестовый email
   - Password: минимум 8 символов
   - Full Name: опционально

4. **Проверьте:**
   - Нет ошибки "Failed to fetch"
   - Появляется сообщение "Check your email"
   - Редирект на `/onboarding`

## 🔍 Если ошибка все еще есть:

1. **Откройте DevTools (F12) → Console:**
   - Проверьте есть ли ошибки
   - Скопируйте полный текст ошибки

2. **Откройте DevTools → Network:**
   - Попробуйте регистрацию снова
   - Найдите запрос к Supabase (обычно `/auth/v1/signup`)
   - Проверьте статус ответа (должен быть 200 или 201)

3. **Проверьте переменные окружения:**
   ```bash
   grep NEXT_PUBLIC_SUPABASE .env.local
   ```
   Должны быть реальные значения, не placeholder.

## 📝 Следующие шаги:

После успешной регистрации:
- ✅ Проверьте email (включая Spam)
- ✅ Нажмите на ссылку подтверждения
- ✅ Должен открыться `/onboarding`

---

**Статус:** ✅ Исправлено  
**Дата:** Январь 2025

