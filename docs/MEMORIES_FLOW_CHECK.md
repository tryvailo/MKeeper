# Проверка цепочки создания и сохранения воспоминаний

## ✅ Исправления внесены

### 1. **Onboarding Page** (`app/onboarding/page.tsx`)
- ✅ **Добавлена загрузка существующих данных** при открытии страницы
- ✅ **Auto-save** работает при переходе между шагами
- ✅ **Финальное сохранение** при submit формы
- ✅ **Логирование** для диагностики

### 2. **Memories Page** (`app/dashboard/memories/page.tsx`)
- ✅ **Загрузка данных** при открытии страницы
- ✅ **Отображение существующих ответов** в полях формы
- ✅ **Сохранение изменений** через кнопку "Save Changes"
- ✅ **Логирование** для диагностики

### 3. **API Route** (`app/api/preferences/route.ts`)
- ✅ **GET** - загружает preferences с interview_data
- ✅ **POST** - сохраняет preferences с interview_data
- ✅ **Логирование** для диагностики

### 4. **Backend Logic** (`lib/api.ts`)
- ✅ **getUserPreferences** - загружает данные из БД
- ✅ **saveUserPreferences** - сохраняет данные в БД
- ✅ **Слияние данных** - сохраняет существующие ответы, которые не были обновлены
- ✅ **Все поля интервью** включены в список (32 поля)
- ✅ **Логирование** для диагностики

## 🔄 Полная цепочка работы

### Шаг 1: Создание воспоминаний (Onboarding)

**Файл:** `app/onboarding/page.tsx`

1. **При открытии страницы:**
   - Проверяется авторизация пользователя
   - Вызывается `loadExistingData()`
   - Загружаются существующие данные из `/api/preferences`
   - Если есть `interview_data`, заполняется `formData`

2. **При заполнении формы:**
   - Пользователь вводит ответы в поля
   - Данные сохраняются в `formData` state
   - При переходе между шагами (step > 1 && step < 6) вызывается `autoSave()`
   - Auto-save отправляет данные в `/api/preferences` (POST)

3. **При финальном сохранении:**
   - Пользователь нажимает "Submit" на шаге Review
   - Вызывается `handleSubmit()`
   - Все данные из `formData` отправляются в `/api/preferences` (POST)
   - После успешного сохранения - редирект на `/onboarding/complete`

**Логи для проверки:**
```
OnboardingPage: Loaded existing preferences: {...}
OnboardingPage: interview_data exists: true/false
OnboardingPage: interview_data keys: [...]
OnboardingPage: Setting formData from existing interview_data
```

### Шаг 2: Сохранение в БД

**Файл:** `app/api/preferences/route.ts` → `lib/api.ts`

1. **POST запрос приходит:**
   - Данные в формате `{ fieldName: "answer", ... }`
   - Вызывается `saveUserPreferences(preferencesData)`

2. **Обработка данных:**
   - Разделение на `interviewFields` и `regularPreferences`
   - Все поля интервью сохраняются в `interviewData`
   - Проверка существующей записи через `getUserPreferences(userId)`

3. **Слияние данных:**
   - Если есть существующие данные → объединяются с новыми
   - Новые данные перезаписывают старые
   - Существующие данные, которые не были обновлены, сохраняются

4. **Сохранение в БД:**
   - Если запись существует → UPDATE
   - Если записи нет → INSERT
   - Данные сохраняются в поле `interview_data` (JSONB)

**Логи для проверки:**
```
POST /api/preferences: Received data for user: ...
POST /api/preferences: Data keys count: ...
saveUserPreferences: Merged interview_data, total keys: ...
saveUserPreferences: Data to save keys: ...
saveUserPreferences: interview_data keys: ...
```

### Шаг 3: Отображение воспоминаний (Memories Page)

**Файл:** `app/dashboard/memories/page.tsx`

1. **При открытии страницы:**
   - Проверяется авторизация пользователя
   - Вызывается `loadPreferences()`
   - Загружаются данные из `/api/preferences` (GET)
   - Если есть `interview_data`, заполняется `formData`
   - Данные отображаются в полях формы

2. **При редактировании:**
   - Пользователь изменяет ответы в полях
   - Данные обновляются в `formData` state
   - Изменения видны сразу в форме

3. **При сохранении:**
   - Пользователь нажимает "Save Changes"
   - Вызывается `handleSave()`
   - Все данные из `formData` отправляются в `/api/preferences` (POST)
   - После успешного сохранения вызывается `loadPreferences()` для обновления данных

**Логи для проверки:**
```
MemoriesPage: Loaded preferences: {...}
MemoriesPage: interview_data exists: true/false
MemoriesPage: interview_data keys: [...]
MemoriesPage: Setting formData from interview_data
MemoriesPage: Saving formData, keys: [...]
MemoriesPage: Save successful, result: {...}
```

### Шаг 4: Загрузка из БД

**Файл:** `lib/api.ts` → `getUserPreferences()`

1. **GET запрос:**
   - Вызывается `getUserPreferences(userId)`
   - Запрос к Supabase: `SELECT * FROM preferences WHERE user_id = ?`
   - Возвращается полная запись с `interview_data`

2. **Возврат данных:**
   - Данные возвращаются в формате:
     ```json
     {
       "id": "...",
       "user_id": "...",
       "interview_data": {
         "happiest_memory": "...",
         "favorite_trip": "...",
         ...
       },
       "created_at": "...",
       "updated_at": "..."
     }
     ```

## 🧪 Как проверить

### Тест 1: Создание новых воспоминаний
1. Откройте `/onboarding`
2. Заполните несколько вопросов (например, 3-5)
3. Перейдите на следующий шаг
4. Проверьте консоль браузера - должны быть логи auto-save
5. Завершите onboarding
6. Перейдите на `/dashboard/memories`
7. **Ожидаемый результат:** Все заполненные ответы должны отображаться

### Тест 2: Редактирование существующих воспоминаний
1. Откройте `/dashboard/memories`
2. Проверьте, что существующие ответы загружены
3. Измените один из ответов
4. Нажмите "Save Changes"
5. Перезагрузите страницу (F5)
6. **Ожидаемый результат:** Изменения должны сохраниться

### Тест 3: Возврат на onboarding
1. Откройте `/onboarding`
2. **Ожидаемый результат:** Все ранее заполненные ответы должны отображаться
3. Добавьте новые ответы
4. Завершите onboarding
5. Проверьте `/dashboard/memories`
6. **Ожидаемый результат:** Все ответы (старые и новые) должны быть сохранены

### Тест 4: Частичное редактирование
1. Откройте `/dashboard/memories`
2. Измените только одно поле
3. Нажмите "Save Changes"
4. Перезагрузите страницу
5. **Ожидаемый результат:** 
   - Измененное поле должно быть обновлено
   - Остальные поля должны остаться без изменений

## 🔍 Диагностика проблем

### Проблема: Данные не загружаются
**Проверьте:**
1. Консоль браузера - логи `MemoriesPage: Loaded preferences` или `OnboardingPage: Loaded existing preferences`
2. Сеть (Network tab) - запрос к `/api/preferences` должен вернуть 200
3. Ответ API - должен содержать `interview_data` с данными

### Проблема: Данные не сохраняются
**Проверьте:**
1. Консоль браузера - логи `MemoriesPage: Saving formData` или `OnboardingPage: Submitting form data`
2. Сеть (Network tab) - запрос POST к `/api/preferences` должен вернуть 200
3. Консоль сервера - логи `saveUserPreferences: Merged interview_data` или `saveUserPreferences: Created new interview_data`
4. База данных - проверьте таблицу `preferences`, поле `interview_data`

### Проблема: Данные теряются при сохранении
**Проверьте:**
1. Логи `saveUserPreferences: Merged interview_data, total keys:` - должно быть больше 0
2. Логи `saveUserPreferences: interview_data keys:` - должны быть все поля
3. Убедитесь, что при сохранении отправляются все поля из `formData`, а не только измененные

## 📋 Список всех полей интервью (32 поля)

1. happiest_memory
2. favorite_trip
3. unforgettable_day
4. proudest_moment
5. biggest_laugh
6. loved_moment
7. best_friend
8. overcame_difficulty
9. met_partner
10. children_young
11. funny_family_moment
12. passed_to_children
13. relationship_parents
14. family_together
15. matters_most
16. most_proud
17. life_lessons
18. brings_peace
19. how_remembered
20. regret_not_telling
21. greatest_strength
22. love_means
23. favorite_hobbies
24. important_people
25. places_love
26. favorite_things
27. personality
28. message_children
29. message_spouse
30. message_grandchildren
31. message_family
32. message_other

## ✅ Все исправления применены

- ✅ Загрузка данных в onboarding при открытии
- ✅ Загрузка данных в memories при открытии
- ✅ Сохранение данных с правильным слиянием
- ✅ Все поля интервью включены в список
- ✅ Логирование для диагностики
- ✅ Сохранение существующих данных при частичном обновлении

Теперь цепочка должна работать полностью: создание → сохранение → отображение → редактирование.

