# Пошаговое руководство: Настройка Supabase Auth в Dashboard

**Дата:** Январь 2025  
**Статус:** Руководство для ручной настройки  
**Время выполнения:** 15-30 минут

---

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

- ✅ Аккаунт Supabase (если нет - создайте на [supabase.com](https://supabase.com))
- ✅ Проект Supabase создан
- ✅ Переменные окружения настроены:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

## 🎯 ШАГ 1: ВХОД В SUPABASE DASHBOARD

### 1.1 Откройте Supabase Dashboard

1. Перейдите на [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. Войдите в свой аккаунт
3. Выберите ваш проект (или создайте новый, если нужно)

### 1.2 Проверьте проект

Убедитесь, что:
- ✅ Проект активен
- ✅ База данных работает
- ✅ У вас есть доступ к настройкам

---

## 🔐 ШАГ 2: НАСТРОЙКА EMAIL PROVIDER

### 2.1 Откройте настройки Authentication

1. В левом меню найдите **"Authentication"** (или **"Auth"**)
2. Нажмите на него
3. В подменю выберите **"Providers"**

### 2.2 Включите Email Provider

1. Найдите секцию **"Email"**
2. Убедитесь, что переключатель **"Enable Email provider"** включен (зеленый)
3. Если выключен - включите его

### 2.3 Настройте Email Confirmation

1. В той же секции найдите **"Confirm email"**
2. **Включите** этот переключатель (рекомендуется для production)
   - Это требует подтверждения email перед входом
   - Повышает безопасность

### 2.4 Настройте Secure Email Change

1. Найдите **"Secure email change"**
2. **Включите** этот переключатель
   - Это требует подтверждения при смене email

**Результат:** Email provider должен быть включен и настроен.

---

## 🌐 ШАГ 3: НАСТРОЙКА URL РЕДИРЕКТОВ

### 3.1 Откройте URL Configuration

1. В меню Authentication выберите **"URL Configuration"**
   - Или перейдите: Authentication → Settings → URL Configuration

### 3.2 Настройте Site URL

1. Найдите поле **"Site URL"**
2. Введите ваш production URL:
   ```
   https://legacywords.co.uk
   ```
   - Или локальный для разработки: `http://localhost:3000`

### 3.3 Добавьте Redirect URLs

1. Найдите секцию **"Redirect URLs"** (или **"Additional Redirect URLs"**)
2. Нажмите **"Add URL"** или **"+"**
3. Добавьте следующие URLs (по одному):

   ```
   https://legacywords.co.uk/dashboard
   https://legacywords.co.uk/onboarding
   https://legacywords.co.uk/auth/callback
   https://legacywords.co.uk/reset-password
   ```

4. Для локальной разработки также добавьте:

   ```
   http://localhost:3000/dashboard
   http://localhost:3000/onboarding
   http://localhost:3000/auth/callback
   http://localhost:3000/reset-password
   ```

5. Нажмите **"Save"** после добавления каждого URL

**Важно:** Без этих URL редиректы после авторизации не будут работать!

---

## 📧 ШАГ 4: НАСТРОЙКА SMTP (ДЛЯ PRODUCTION)

### 4.1 Выбор SMTP провайдера

**Рекомендуется:** Resend (бесплатный tier: 3,000 emails/месяц)

Альтернативы:
- SendGrid (100 emails/день бесплатно)
- Postmark (100 emails/месяц бесплатно)
- AWS SES (62,000 emails/месяц бесплатно)

### 4.2 Настройка Resend (рекомендуется)

#### 4.2.1 Создайте аккаунт Resend

1. Перейдите на [resend.com](https://resend.com)
2. Нажмите **"Sign Up"**
3. Создайте аккаунт (можно через GitHub)
4. Подтвердите email

#### 4.2.2 Получите API ключ

1. После входа перейдите в **"API Keys"** (в меню слева)
2. Нажмите **"Create API Key"**
3. Введите название: `Memory Keeper Production`
4. Выберите права: **"Full Access"** (или только отправка email)
5. Нажмите **"Add"**
6. **Скопируйте API ключ** (он показывается только один раз!)
   - Формат: `re_xxxxxxxxxxxxx`

#### 4.2.3 Настройте домен (опционально, но рекомендуется)

1. В Resend перейдите в **"Domains"**
2. Нажмите **"Add Domain"**
3. Введите ваш домен: `legacywords.co.uk`
4. Следуйте инструкциям для настройки DNS записей:
   - Добавьте TXT записи для DKIM
   - Добавьте MX записи (если нужно)
5. Подождите верификации (обычно несколько минут)

**Примечание:** Можно использовать и без кастомного домена, но deliverability будет ниже.

### 4.3 Настройка SMTP в Supabase

1. В Supabase Dashboard перейдите:
   - **Settings** (внизу слева) → **Auth** → **SMTP Settings**
   - Или: Authentication → Settings → SMTP

2. Включите **"Custom SMTP"** (переключатель)

3. Заполните поля:

   **Для Resend:**
   ```
   SMTP Host: smtp.resend.com
   SMTP Port: 465 (или 587)
   SMTP User: resend
   SMTP Password: [ваш API ключ из Resend]
   Sender Email: no-reply@legacywords.co.uk
   Sender Name: Memory Keeper
   ```

   **Для SendGrid:**
   ```
   SMTP Host: smtp.sendgrid.net
   SMTP Port: 587
   SMTP User: apikey
   SMTP Password: [ваш SendGrid API ключ]
   Sender Email: no-reply@legacywords.co.uk
   Sender Name: Memory Keeper
   ```

4. Нажмите **"Save"**

5. **Протестируйте отправку:**
   - В Supabase Dashboard → Authentication → Users
   - Найдите тестового пользователя или создайте нового
   - Нажмите **"Send magic link"** или **"Resend confirmation email"**
   - Проверьте, что email пришел

**Важно:** 
- Для production **обязательно** настройте кастомный SMTP!
- Без SMTP Supabase будет отправлять только на адреса команды проекта

---

## ✉️ ШАГ 5: КАСТОМИЗАЦИЯ EMAIL ШАБЛОНОВ

### 5.1 Откройте Email Templates

1. В Supabase Dashboard перейдите:
   - **Authentication** → **Email Templates**
   - Или: Authentication → Settings → Email Templates

### 5.2 Обновите шаблон "Confirm signup"

1. Найдите шаблон **"Confirm signup"** (или **"Confirmation"**)
2. Нажмите на него для редактирования

3. Обновите **Subject** (тема письма):
   ```
   Confirm your Memory Keeper account
   ```

4. Обновите **Body** (тело письма):
   ```html
   <h2>Welcome to Memory Keeper</h2>
   <p>Thank you for signing up! Please confirm your email address to get started.</p>
   <p><a href="{{ .ConfirmationURL }}">Confirm your email address</a></p>
   <p>Or enter this code: {{ .Token }}</p>
   <p>This link will expire in 1 hour.</p>
   <hr>
   <p style="color: #666; font-size: 12px;">
     If you didn't create an account, you can safely ignore this email.
   </p>
   ```

5. Нажмите **"Save"**

### 5.3 Обновите шаблон "Magic Link"

1. Найдите шаблон **"Magic Link"**
2. Обновите **Subject**:
   ```
   Your Memory Keeper login link
   ```

3. Обновите **Body**:
   ```html
   <h2>Sign in to Memory Keeper</h2>
   <p>Click the link below to sign in to your account:</p>
   <p><a href="{{ .ConfirmationURL }}">Sign in to Memory Keeper</a></p>
   <p>Or enter this code: {{ .Token }}</p>
   <p>This link will expire in 1 hour.</p>
   <hr>
   <p style="color: #666; font-size: 12px;">
     If you didn't request this, you can safely ignore this email.
   </p>
   ```

4. Нажмите **"Save"**

### 5.4 Обновите шаблон "Reset Password"

1. Найдите шаблон **"Reset Password"** (или **"Recovery"**)
2. Обновите **Subject**:
   ```
   Reset your Memory Keeper password
   ```

3. Обновите **Body**:
   ```html
   <h2>Reset your password</h2>
   <p>We received a request to reset your password. Click the link below to set a new password:</p>
   <p><a href="{{ .ConfirmationURL }}">Reset password</a></p>
   <p>Or enter this code: {{ .Token }}</p>
   <p>This link will expire in 1 hour.</p>
   <hr>
   <p style="color: #666; font-size: 12px;">
     If you didn't request a password reset, you can safely ignore this email.
   </p>
   ```

4. Нажмите **"Save"**

### 5.5 Обновите шаблон "Change Email Address"

1. Найдите шаблон **"Change Email Address"**
2. Обновите **Subject**:
   ```
   Confirm your new email address
   ```

3. Обновите **Body**:
   ```html
   <h2>Confirm email change</h2>
   <p>You requested to change your email address from {{ .Email }} to {{ .NewEmail }}.</p>
   <p><a href="{{ .ConfirmationURL }}">Confirm email change</a></p>
   <p>Or enter this code: {{ .Token }}</p>
   <hr>
   <p style="color: #666; font-size: 12px;">
     If you didn't request this change, please contact support immediately.
   </p>
   ```

4. Нажмите **"Save"**

**Примечание:** Вы можете добавить свой логотип, цвета и брендинг в шаблоны.

---

## 🛡️ ШАГ 6: НАСТРОЙКА RATE LIMITS (ОПЦИОНАЛЬНО)

### 6.1 Откройте Rate Limits

1. В Supabase Dashboard перейдите:
   - **Authentication** → **Rate Limits**
   - Или: Authentication → Settings → Rate Limits

### 6.2 Настройте лимиты

По умолчанию лимиты:
- Magic Link: 1 запрос в 60 секунд
- OTP: 1 запрос в 60 секунд
- Password Reset: 1 запрос в 60 секунд

**Рекомендация:** Оставьте значения по умолчанию для начала. Можно изменить позже при необходимости.

---

## 🤖 ШАГ 7: НАСТРОЙКА CAPTCHA (РЕКОМЕНДУЕТСЯ ДЛЯ PRODUCTION)

### 7.1 Выбор CAPTCHA провайдера

**Рекомендуется:** Google reCAPTCHA v3 (невидимая)

Альтернативы:
- hCaptcha
- Cloudflare Turnstile

### 7.2 Настройка Google reCAPTCHA v3

#### 7.2.1 Создайте reCAPTCHA ключи

1. Перейдите на [Google reCAPTCHA Admin](https://www.google.com/recaptcha/admin)
2. Войдите в Google аккаунт
3. Нажмите **"+"** (Create)
4. Заполните форму:
   - **Label:** Memory Keeper
   - **reCAPTCHA type:** Выберите **"reCAPTCHA v3"**
   - **Domains:** Добавьте:
     - `legacywords.co.uk`
     - `localhost` (для разработки)
   - Примите Terms of Service
5. Нажмите **"Submit"**
6. **Скопируйте Site Key и Secret Key**
   - Site Key: `6Le...` (публичный, можно использовать в коде)
   - Secret Key: `6Le...` (приватный, только для сервера)

#### 7.2.2 Настройте в Supabase

1. В Supabase Dashboard перейдите:
   - **Authentication** → **Providers** → **Email**
   - Прокрутите вниз до секции **"CAPTCHA"**

2. Включите **"Enable CAPTCHA"**

3. Выберите провайдера: **"Google reCAPTCHA v3"**

4. Введите ключи:
   - **Site Key:** [ваш Site Key]
   - **Secret Key:** [ваш Secret Key]

5. Нажмите **"Save"**

**Примечание:** CAPTCHA защищает от ботов и спам регистраций. Рекомендуется для production.

---

## ✅ ШАГ 8: ПРОВЕРКА НАСТРОЕК

### 8.1 Проверьте все настройки

Убедитесь, что:

- [ ] Email provider включен
- [ ] Email confirmation включен
- [ ] Site URL настроен
- [ ] Redirect URLs добавлены
- [ ] SMTP настроен (для production)
- [ ] Email шаблоны обновлены
- [ ] CAPTCHA настроен (опционально)

### 8.2 Протестируйте регистрацию

1. Откройте ваше приложение: `http://localhost:3000/sign-up`
2. Заполните форму регистрации
3. Нажмите **"Create Account"**
4. Проверьте email (включая папку Spam)
5. Нажмите на ссылку подтверждения
6. Убедитесь, что редирект работает на `/onboarding`

### 8.3 Протестируйте вход

1. Откройте `/sign-in`
2. Введите email и password
3. Нажмите **"Sign in"**
4. Убедитесь, что редирект работает на `/dashboard`

### 8.4 Протестируйте сброс пароля

1. Откройте `/forgot-password`
2. Введите email
3. Проверьте email
4. Нажмите на ссылку
5. Убедитесь, что открывается `/reset-password`
6. Введите новый пароль
7. Убедитесь, что можно войти с новым паролем

---

## 🔍 ШАГ 9: ПРОВЕРКА ЛОГОВ И ОШИБОК

### 9.1 Проверьте логи в Supabase

1. В Supabase Dashboard перейдите:
   - **Logs** → **Auth Logs**
2. Проверьте, что нет ошибок при регистрации/входе

### 9.2 Проверьте логи в приложении

1. Откройте консоль браузера (F12)
2. Проверьте, что нет ошибок в консоли
3. Проверьте Network tab - все запросы должны быть успешными

### 9.3 Проверьте логи на сервере

Если используете Vercel:
1. Откройте Vercel Dashboard
2. Перейдите в **Deployments** → выберите последний деплой
3. Откройте **Logs**
4. Проверьте, что нет ошибок

---

## 🚨 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Email не приходит

**Решения:**
1. Проверьте папку Spam
2. Убедитесь, что SMTP настроен правильно
3. Проверьте логи в Supabase Dashboard → Logs → Auth Logs
4. Убедитесь, что email не в списке заблокированных в Supabase

### Проблема: Редирект не работает

**Решения:**
1. Убедитесь, что URL добавлен в Redirect URLs
2. Проверьте, что Site URL правильный
3. Убедитесь, что URL точно совпадает (включая http/https)

### Проблема: "Invalid redirect URL"

**Решения:**
1. Проверьте, что URL добавлен в Redirect URLs в Supabase
2. Убедитесь, что URL в коде совпадает с настройками
3. Проверьте, что нет лишних слешей или параметров

### Проблема: "Email address not authorized"

**Решения:**
1. Это означает, что SMTP не настроен
2. Настройте кастомный SMTP (см. Шаг 4)
3. Или добавьте email в список авторизованных в Team settings

### Проблема: CAPTCHA не работает

**Решения:**
1. Убедитесь, что ключи правильные
2. Проверьте, что домен добавлен в reCAPTCHA
3. Проверьте консоль браузера на ошибки

---

## 📝 ЧЕКЛИСТ ЗАВЕРШЕНИЯ

После выполнения всех шагов проверьте:

- [ ] ✅ Email provider включен
- [ ] ✅ Email confirmation включен
- [ ] ✅ Site URL настроен
- [ ] ✅ Все Redirect URLs добавлены
- [ ] ✅ SMTP настроен и протестирован
- [ ] ✅ Email шаблоны обновлены
- [ ] ✅ CAPTCHA настроен (опционально)
- [ ] ✅ Регистрация работает
- [ ] ✅ Вход работает
- [ ] ✅ Сброс пароля работает
- [ ] ✅ Email отправляются корректно
- [ ] ✅ Нет ошибок в логах

---

## 🎉 ГОТОВО!

После выполнения всех шагов ваша авторизация через Supabase Auth должна работать полностью!

**Следующие шаги:**
1. Протестируйте все функции авторизации
2. Проверьте работу на production
3. Настройте мониторинг (опционально)

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [Supabase Auth Documentation](https://supabase.com/docs/guides/auth)
- [Email Templates Guide](https://supabase.com/docs/guides/auth/auth-email-templates)
- [SMTP Setup Guide](https://supabase.com/docs/guides/auth/auth-smtp)
- [Resend Documentation](https://resend.com/docs)
- [Google reCAPTCHA Documentation](https://developers.google.com/recaptcha/docs/v3)

---

**Дата создания:** Январь 2025  
**Статус:** Готово к использованию

